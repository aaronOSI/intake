// import IncidentInformationCardView from 'components/screenings/IncidentInformationCardView'
// import React from 'react'
// import {mount} from 'enzyme'

// xdescribe('IncidentInformationCardView', () => {
  // let component
  // beforeEach(() => {
    // component = mount(<IncidentInformationCardView />)
  // })
  // xdescribe('when mode is set to show', () => {
    // it('renders the incident show card', () => {
      // expect(component.find('IncidentInformationShowView').length).toEqual(1)
    // })
  // })
  // xdescribe('when mode is set to edit', () => {
    // it('renders the incident edit card', () => {
      // expect(component.find('IncidentInfomationEditView').length).toEqual(1)
    // })
  // })
// })
